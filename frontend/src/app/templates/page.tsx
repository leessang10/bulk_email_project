"use client";
import { Typography } from "antd";

export default function TemplatesPage() {
  return (
    <div>
      <Typography.Title level={2}>이메일 템플릿 관리</Typography.Title>
      <p>이곳에서 이메일 템플릿을 생성, 수정, 삭제할 수 있습니다.</p>
    </div>
  );
}
