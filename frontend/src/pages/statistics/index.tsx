import styled from "styled-components";

const StatisticsContainer = styled.div`
  padding: 20px;
`;

const StatisticsPage = () => {
  return (
    <StatisticsContainer>
      <h1>이메일 발송 통계</h1>
      {/* 통계 컴포넌트들이 여기에 들어갈 예정 */}
    </StatisticsContainer>
  );
};

export default StatisticsPage;
